---
import BlogCard from '../../components/BlogCard.astro'
import BaseLayout from '../../layouts/BaseLayout.astro'

import data from '../../../public/posts.json'
const posts = data.docs.sort(
  (a, b) => new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime()
)
---

<BaseLayout>
	<main class="app-container pt-36">
		<h1 class="text-6xl pb-8 font-bold">Blog Educativo Pucará</h1>
		<p class="md:w-2/3 text-lg leading-[2rem]">
			En el blog de Veterinaria Pucará encontrarás articulos con consejos de cuidado, prevención y bienestar para tu mascota
			además de noticias del mundo veterinario y curiosidades que te ayudarán a conocerla mejor.
		</p>
	</main>
	<section class="app-container py-12">
		<ul class="posts-grid-container">
			{
				posts.map((post) => (
					<li>
						<BlogCard post={post} />
					</li>
				))
			}
		</ul>
	</section>
</BaseLayout>
